version: 2

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/zh-CN/conf.py

# Set the version of Python and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  jobs:
    pre_build:
      # Generate on-the-fly Sphinx configuration from Jupyter Book's _config.yml
      - "jupyter-book config sphinx docs/zh-CN"
      - "jupyter-book build docs/zh-CN"
    build:
      html:
        - "jupyter-book build docs/zh-CN"
        - "cp -r docs/zh-CN/_build/* $READTHEDOCS_OUTPUT"

python:
  install:
  - requirements: docs/requirements-doc.txt
  # - method: pip
  #   path: ".[docs]"
